// Copy this file to config.js and fill in your real values.
window.ADMIN_CONFIG = {
  apiBaseUrl: "https://api.example.com", // trailing slash not needed
  authToken: "" // e.g., "Bearer abc123" — leave empty if not required
};
